# Job #110 — Changelog

## Added
- `Workspace.EndBase` — the river's end landing basin at z 18000–18900: 1.34M terrain voxels and 1597
  props, a 180°-rotated copy of `SpawnBase`.

## Notes
- Place-file content only; no code changed in this job.
- `SpawnLocation`, `Plane` and the two `Dock` markers were deliberately stripped from the copy.
