# TODO 0037: REQUIRED (Job 068 E2): Dock water-lapping sound never attaches -- LobbySoundscape looks up the wrong path

**Project:** `roblox.jungle`
**Status:** resolved (2026-08-02) — Fixed in Job 069, 2026-08-02. LobbySoundscape.server.luau now does dock:FindFirstChild("Pier", true) -- a RECURSIVE find by name -- instead of the non-recursive lookup that returned nil. Also added a warn() on the else branch so a future re-nesting fails loudly instead of silently dropping the sound. Deliberately NOT hardcoded to Scenery.Dock.Dock.Pier: the double-Dock is an artifact of how the Store asset was inserted, not a contract, so a name search at any depth survives the dock being swapped again. VERIFIED live via Studio MCP: OLD lookup returns nil (confirming the bug was real), NEW lookup returns Workspace.LOBBY_GREYBOX.Scenery.Dock.Dock.Pier, a Part, isBasePart=true. There were 0 Sound instances under Scenery.Dock beforehand, confirming the water loop had never been created. Regression check passed: FirePit rule still finds 2, both watchtower lookups still resolve to a BasePart. Analyzer clean. NOT yet heard in-game -- needs a Rojo sync and a Play; the script prints its own water x N count, so the check is that it reports water x1.
**Created:** 2026-08-02 11:32:33

Found by the Job 068 live sweep, 2026-08-02. LobbySoundscape.server.luau:100-104 does Scenery.Dock:FindFirstChild("Pier"). The Store dock model is nested one level deeper -- the real path is Scenery.Dock.Dock.Pier -- so the lookup returns nil, waterCount stays 0, and NO water-lapping loop is ever created at the dock. ASSETS.md 1.11 claims it is wired; it is not. Invisible from disk, only the live tree shows it. One-line fix: FindFirstChild("Pier", true) for a recursive search, or re-point at the nested model. Verify by re-running and checking the [LobbySoundscape] print reports water x1.
