# TODO 0034: OPTIONAL (Job 068 D2/D3): Two lobby perf micro-notes -- Heartbeat pad tick, full-Workspace station scans

**Project:** `roblox.jungle`
**Status:** resolved (2026-08-02) — CLOSED AS NO-ACTION -- user decision 2026-08-02, after re-measuring against the live place. D2 (LobbyServer Heartbeat pad tick): runs every frame but gates to real work every 0.3s, then does #pads x #Players distance checks = 4 x 6 = 24 cheap Vector3 magnitude comparisons six times a second. Free. D3 (full-Workspace GetDescendants scans in LobbyStations and LobbyServer, retried every 0.2s for up to 15s): the place has 3,810 BaseParts so the walk is genuinely large, BUT it is startup-only, five callers, and the Play console shows all four stations binding immediately with no stall. Neither is perceptible; restructuring how two working money-adjacent startup scripts find their stations would cost more in risk than it returns. WORTH RECORDING: the audit DID find a real perf item in this area and it was neither of these -- the doubled Bloom/SunRays post-effects (todo 0039), where two extra full-screen passes cost far more on mobile than any amount of Luau distance-checking. That is fixed. If lobby perf is ever actually reported as a problem, profile first (rbx-perf-profiling / MicroProfiler) rather than reopening these two guesses.
**Created:** 2026-08-02 10:51:17

Audit Job 068, gaps D2 and D3. Neither needs action now; recorded because GAME.md mobile perf is a standing constraint. D2: LobbyServer:379 runs an unconditional Heartbeat, gated to real work every 0.3s, doing #pads x #Players distance checks. Fine at lobby scale. D3: LobbyStations:33 and LobbyServer:38 each do a full Workspace:GetDescendants() scan, retried every 0.2s for up to 15s at startup, five callers. Startup-only and invisible to players. Only worth touching if the lobby place grows a lot.
