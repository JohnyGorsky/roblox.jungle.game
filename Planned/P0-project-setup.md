# P0 — Set up Jungle project (tech prerequisite)

**Source:** Job 001 roadmap. **Blocks:** everything.

Create the on-disk `sync/` structure + `default.project.json` so Studio Sync stops erroring, the
`luau-analyze` wrapper + generated `sourcemap.json`, and fill in the `jungle-project` skill
(architecture, sync table already recorded). Base the layout on the live "Jungle run" place (readable
via the Studio MCP).

→ Promote to a job first.
