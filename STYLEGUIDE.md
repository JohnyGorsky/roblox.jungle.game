# STYLEGUIDE.md — Last River (Jungle)

> **Status: SKELETON — no values filled in yet.** This is the structure only. We fill each section
> during the design pass (Job #062), then promote the accepted result into a reusable skill so every
> future design task builds to it. Until a section is filled, treat it as `_TBD_`.
>
> This is the game's **single source of truth for look, feel, and design rules** — colors, art
> direction, principles, and the do/don't rules every GUI, model, effect, and system is built to.
> It sits *above* the engine skills (`roblox-ui`, `roblox-vfx`, `roblox-audio`, …): those say *how*
> to build in Roblox; this says *what this game should look and feel like*.

---

## 1. Purpose & how to use

_Who reads this, when, and the rule that new work must match it. How this relates to the (future)
per-game GUI skill and the shared engine skills. When a value is missing here, ask — don't invent it._

`_TBD_`

## 2. Design pillars / principles

_The 3–6 north-star statements that every decision is checked against (the game's identity in words —
e.g. tension, mobile-first, readability, low-poly charm). Each pillar: a name + one line + what it
rules in / out._

`_TBD_`

## 3. Art direction

_The overall visual style (e.g. stylized low-poly), silhouette/shape language, level of detail,
mood/tone, and the "does this belong?" test for any new model or scene._

`_TBD_`

## 4. Color palette

_Core / brand colors, semantic colors (health, fuel, ammo, danger, success…), and environment
palettes (jungle, water, night). Every color as `Color3.fromRGB(r, g, b)` with a name and where it's
used. Include contrast/accessibility notes._

`_TBD_`

## 5. Typography

_Fonts (primary / secondary / display), the text size hierarchy, stroke/outline rules, and
mobile-readability rules (TextScaled, min tap/read sizes)._

`_TBD_`

## 6. UI / HUD style

_Container/panel style, buttons, progress bars, HUD layout, spacing/corner/stroke tokens, and the
mobile-first scaling rules (UDim2 scale, safe-area, aspect constraints). This section is the seed of
the game's GUI-design skill._

`_TBD_`

## 7. Iconography

_Icon style, sizing, source (Flaticon per GROUND-RULES §4), and the asset-ID table for icons already
uploaded to Roblox._

`_TBD_`

## 8. Lighting & VFX

_Lighting mood, day/night look, fog/atmosphere/sky, and the vocabulary of particles/beams/trails
(muzzle flash, water spray, impacts) — with mobile-perf budgets._

`_TBD_`

## 9. Audio direction

_Overall sonic tone, music style/adaptivity, SFX character, and 3D/spatial rules (engine, animals,
gunfire). Sourcing per GROUND-RULES §4 (Pixabay). Balance/mix targets._

`_TBD_`

## 10. Materials & textures

_Surface/material language for terrain, boat, props, water; texture resolution/style budget; how
Meshy-generated and Creator-Store assets are made to fit the palette and art direction._

`_TBD_`

## 11. Naming & conventions

_Naming rules for GUI instances, models, folders, and assets so everything stays consistent and
greppable._

`_TBD_`

## 12. Rules (do / don't)

_The hard design rules — the things we always do and never do (colors off-palette, off-style models,
pixel-fixed UI, etc.). This is what a review checks against._

`_TBD_`

## 13. References (north-star)

_Visual/design references we're steering toward (e.g. Dead Rails feel), mood boards, and links.
What we borrow vs. what we deliberately do differently._

`_TBD_`

## 14. Quality checklist

_The pre-delivery checklist any new visual/design work is run through before it's considered done._

- [ ] `_TBD — filled during the design pass_`
